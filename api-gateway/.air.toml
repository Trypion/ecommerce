root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/api-gateway"
  cmd = "go build -o ./tmp/api-gateway ./cmd/main.go"
  include_ext = ["go"]
  exclude_regex = ["_test.go"]
  exclude_dir = ["tmp", "vendor"]
  delay = 1000

[screen]
  keep_scroll = true