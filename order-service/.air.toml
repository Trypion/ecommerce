root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/order-service"
  cmd = "go build -o ./tmp/order-service ./cmd/main.go"
  include_ext = ["go"]
  exclude_regex = ["_test.go"]
  exclude_dir = ["tmp", "vendor"]
  delay = 1000

[screen]
  keep_scroll = true